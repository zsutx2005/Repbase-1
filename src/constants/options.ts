/**
 * instance default params
 *
 */

export type Options = {
  repository: string,
  style?: string,
  dataType?: string,
}

export const options: Options = {
  repository: '',
  style: 'network',
  dataType: 'string',
}

